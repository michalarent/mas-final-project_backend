package com.example.mas_final_project.repository;

import com.example.mas_final_project.model.RequestedReport;
import org.springframework.data.repository.CrudRepository;

public interface RequestedReportRepository extends CrudRepository<RequestedReport, Long> {
}
